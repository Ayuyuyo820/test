# 需求
# 0. 给定一个整数列表，请编程来调整该列表中整数的顺序，使得所有奇数排好序后放在数组的前半部分，所有偶数排好序后放在数组的后半部分。
# 比如给定的整数列表是 [1, 8, 7, 3, 6, 5, 4, 2]，那么调整后的结果应该是 [1, 3, 5, 7, 2, 4, 6, 8]。

s = input("请输入一个数")
# 按照逗号分割字符串
int_s = s.split(',')
number = []
# 遍历然后转为整数
for numbers in int_s:
    number.append(int(numbers))
# 存放奇数和偶数
odds = []
evens = []
# 判断奇数偶数
for i in number:
    if i % 2 == 1:
        odds.append(i)
    else:
        evens.append(i)

 # 排序   
odds.sort()
evens.sort()
# 拼接
alls = odds + evens
# 输出
print(alls)
