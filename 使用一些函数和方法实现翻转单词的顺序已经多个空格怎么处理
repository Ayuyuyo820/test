# 1. 翻转单词顺序。
# 用户输入一个英文句子，请编程翻转句子中单词的顺序，但单词内字符的顺序不变。为简单起见，标点符号和普通字母一样处理。
# 例如输入字符串是 "I love FishC."，则输出 "FishC. love I"。
# 注意1：用户输入的字符串可能会在前面或者后面包含任意数量的空格，但是反转后的结果将会去除这些空格
# （例如输入字符串是 "   I love FishC.   "，结果依然输出 "FishC. love I"）
# 注意2：用户输入的字符串中，单词之间可能不止一个空格，但是反转后的结果将统一使用一个空格作为单词之间的间隔
# （例如输入字符串是 "I   love        FishC."，结果依然输出 "FishC. love I"）

# 获取用户输入的句子
s = input("请输入一句话")

# 去除字符串首尾的空格（原代码错误使用了strpt()，正确为strip()）
ss = s.strip()

# 按空格分割字符串，自动处理单词间的多个空格，返回单词列表
sss = ss.split()

# 翻转单词列表的顺序（例如 ["I", "love", "you"] 变为 ["you", "love", "I"]）
sss.reverse()

# 用单个空格连接翻转后的单词列表，形成最终字符串并打印
print(' '.join(sss))
